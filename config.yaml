projects:
  - name: "sample-project"
    repoUrl: "https://github.com/user/sample-project.git"
    env:
      PROD: "https://prod-api.example.com"
      PRE: "https://pre-api.example.com"
      TEST: "https://test-api.example.com"
      DEV: "https://dev-api.example.com"
    # verify_ssl: true  # Optional: Enable/disable SSL certificate verification (default: true)
    # use_version_fallback: true  # Optional: Use version as commit reference if git commit not found (default: true)
    # jira_base_url: "https://yourcompany.atlassian.net"  # Optional: JIRA base URL for linking tickets found in commit messages
  
  # Example with version fallback disabled (strict git commit requirement)
  # - name: "strict-git-service"
  #   repoUrl: "https://github.com/company/strict-service.git"
  #   env:
  #     PROD: "https://strict-prod.company.com"
  #     DEV: "https://strict-dev.company.com"
  #   use_version_fallback: false  # Require actual git commit ID, don't use version as fallback
  
  # Example with SSL verification disabled (useful for internal environments with self-signed certificates)
  # - name: "internal-service"
  #   repoUrl: "https://github.com/company/internal-service.git"
  #   env:
  #     PROD: "https://internal-prod.company.local"
  #     PRE: "https://internal-pre.company.local"
  #     TEST: "https://internal-test.company.local"
  #     DEV: "https://internal-dev.company.local"
  #   verify_ssl: false  # Disable SSL verification for self-signed certificates
  #   use_version_fallback: true   # Use version as git tag if commit ID not available
  
  # Add more projects as needed
  # - name: "another-project"
  #   repoUrl: "https://github.com/user/another-project.git"
  #   env:
  #     PROD: "https://prod-api2.example.com"
  #     PRE: "https://pre-api2.example.com"
  #     TEST: "https://test-api2.example.com"
  #     DEV: "https://dev-api2.example.com"